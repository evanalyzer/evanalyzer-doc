---
title: Downloads
description: Find the correct EVAnalyzer download for your operating system, with or without CUDA acceleration.
---

import DownloadPicker from "../../../components/DownloadPicker.astro";

Pick your operating system and whether you want a CUDA-accelerated build. The link below always points at the latest published release on [GitHub](https://github.com/evanalyzer/evanalyzer/releases).

<DownloadPicker />

:::tip[Not sure which to pick?]
If you don't have a dedicated NVIDIA GPU, choose **No CUDA** - the CPU build runs everywhere and AI segmentation still works, just slower. See [System Requirements](/getting-started/system-requirements/) for GPU/driver details, and [Installation](/getting-started/installation/) for how to extract and start the app once it's downloaded.
:::

:::note[Linux + CUDA is two files]
The Linux CUDA build is published as a two-part archive (`.7z.001` and `.7z.002`) because of GitHub's 2 GB file-size limit. Download both parts shown above into the same folder before extracting - see [Installation](/getting-started/installation/) for the extraction command.
:::

## VS Code Extension

The [EVAnalyzer File Support](/vscode/extension/) extension adds validation, autocomplete, and creation wizards for `.evapipe`, `.evaproj`, and `.evapt` files to VS Code.

**[Download the latest release →](https://github.com/evanalyzer/evanalyzer-vscode/releases/latest)**

Grab the `evanalyzer-file-support-*.vsix` file from the release's assets, then install it:

```sh
code --install-extension evanalyzer-file-support-*.vsix
```

:::note[Not yet on the Marketplace]
This extension isn't published to the VS Code Marketplace or Open VSX yet, so it's installed from the downloaded `.vsix` file rather than through the Extensions view's search. You can also install it via **Extensions → ... → Install from VSIX...** in VS Code - see [VS Code Extension](/vscode/extension/#installation) for details.
:::
